# DSH Remote / DeepSeek API 软件界面要求

这份文档是后续修改 UI、交互和资源的唯一执行基准。新增页面或改动已有页面前，先对照本文件和 `ui-asset-library.md`；旧玻璃态 Dashboard 的字号、图标和布局规则不作为新界面参考。

## 1. 产品边界与数据入口

- API 聊天、API 项目和普通 API 最近聊天属于 `magic5-chat`，使用独立数据与本地项目存储。
- Remote 首页、Remote 项目和 Remote 聊天属于根 `index.html`，只通过 Remote 数据/桥接通信。
- API 项目聊天只能从 API 项目详情页进入，不出现在普通 API 最近聊天。
- Remote 首页只展示设备与项目列表；Remote 最近聊天不在首页显示。
- API 顶部胶囊显示聊天名称；只有归属项目的聊天显示项目名，没有项目的普通聊天不显示项目行。
- 模型与思考等级必须是两个独立选择器：点击模型只改变模型，点击思考等级只改变思考等级。
- Remote 的连接、设备、项目和聊天逻辑不能被 API 页面复用或反向修改。

## 2. 字体层级

| 层级 | 字号 | 字重 | 行高 | 颜色 | 使用范围 |
| --- | ---: | ---: | ---: | --- | --- |
| Remote 导航标题 | 21px | 700 | 1.1 | `#111214` | Remote 顶部标题 |
| 页面大标题 | 30px | 700 | 1.1 | `#111214` | 项目、设置等独立页面 |
| 分区标题 | 16px | 700 | 1.2 | `#101114` | 项目、版本信息、设备连接 |
| 项目目录名称 | 20px | 600 | 1.15 | `#101114` | 项目列表 |
| 详情标题 | 18px | 600 | 1.2 | `#101114` | 项目/聊天详情 |
| 聊天胶囊标题 | 14px | 700 | 1.2 | `#101114` | 顶部聊天名称 |
| 正文 | 16px | 400 | 1.45–1.55 | `#111214` | 聊天消息、主要内容 |
| 主要按钮 | 16px | 600 | 1.2 | `#fff` | 蓝色主操作 |
| 次要按钮/菜单 | 14–15.5px | 600 | 1.2–1.25 | `#111214` | 选择项、功能菜单 |
| 辅助文字 | 12px | 400 | 16–18px | `#85898d` | 日期、说明、版本备注 |
| 状态文字 | 12–13px | 400–600 | 16–18px | `#606369` | 在线、连接、重试状态 |

只使用 400、500、600、700 四档字重。300 仅可用于装饰性的加号或细线箭头；禁止引入 550、580、650、680、720 等中间字重。中文与英文优先使用 `HONOR Sans CN`，数字沿用同一字体。

## 3. 颜色、尺寸和表面

| 项目 | 统一值 |
| --- | --- |
| 主文字 | `#111214` |
| 次文字 | `#85898d` |
| 辅助深文字 | `#606369` |
| 边框/分割线 | `#e5e7e9` |
| 主操作蓝 | `#3286f5` |
| 成功绿 | `#48b96a` |
| 危险红 | `#e32935` |
| 页面背景 | `#fff` 或极浅灰白 |
| 搜索框 | 高 52–54px，圆角 27–28px |
| 菜单/弹层 | 圆角 22–25px，白底，轻阴影 |
| 菜单行 | 最小高 46px，圆角 14px |
| 触控按钮 | 最小 46×46px，圆形按钮优先 |
| 项目图标底板 | 58×58px，圆角 18px，`#f1f1f1` |

阴影统一使用 `0 4px 18px rgba(29,36,40,.08)` 一类的轻、宽松阴影；不使用深黑重阴影、玻璃高光或发光边缘。

## 4. 图标和资产库规则

- 所有图标先查 `ui-asset-library.md`，同一用途只能有一个当前素材。
- 优先使用 Remote 现有 16/24 viewBox SVG；线性图标统一 `#151719`、约 1.8–2px、round cap/join。
- 文件夹、电脑、HONOR 等品牌/实体图标可使用现有实心素材，但不得拿文件夹代替文件、拿照片代替文档。
- “添加项目”使用文件夹加号图标；项目列表本身使用普通文件夹图标；“添加文件/已上传文件”使用文档图标；“添加照片/上传照片”使用照片图标，四者不可混用。
- 置顶、复制、重命名、查找、归档、删除分别对应独立语义图标；归档危险态使用 Remote 的 `archive-red.svg`。
- 上下文剩余、本对话 token、本轮费用必须使用三枚独立图标，不能共用工具、系统提示词或对话信息图标。
- 只有资产库确认没有合规素材时，才用 Image 2 生成透明 PNG；生成后需检查透明背景、光学边界、20px 显示清晰度，再登记入库。
- 图标显示尺寸通常为 20–24px；项目列表文件夹按页面需要放大到 21–31px；图标不得通过 CSS 随意拉伸变形。

## 5. 关键交互要求

- 返回按钮只返回上一级页面，不允许出现双层退出或重复导航头。
- API 侧栏底部必须同时提供“新建对话”和“设置”两个并排圆形按钮；新建对话在设置左侧，保证入口可发现且左右对称。
- 顶部三个点打开当前页面对应菜单；API 菜单不得跳到 Remote 设置，Remote 菜单不得跳到 API 设置。
- API 功能菜单保留置顶、添加到项目、已上传文件、聊天中查找、归档、删除；不添加无关工具项。
- 添加文件和添加照片必须进入不同选择流程；上传文件卡片不得使用照片缩略图图标。
- 上下文弹层使用一列三行：上下文剩余、本对话 token、本轮费用；不做四宫格，不展示系统提示词、工具提示词或缓存数。
- 项目列表显示项目名称和时间；普通聊天列表不混入项目内聊天。

## 6. 发布前检查

1. 对照资产库检查每个 `<img>` 的用途和路径，确认文件、照片、项目动作分别使用文档、照片、文件夹加号素材。
2. 检查字体只使用允许的字重和层级，标题、正文、辅助文字颜色符合表格。
3. 在 Magic5 Pro 尺寸浏览器检查安全区、胶囊、弹层、输入栏和项目日期。
4. 分别验证普通 API 聊天、API 项目、Remote 首页、Remote 项目、设置页。
5. 运行两个入口脚本的语法检查，并执行 `git diff --check`。
6. 发布 APK 后同步更新 `dist/update.json` 的版本号。

## 7. 修改协议

每次修改必须记录：修改页面、使用的资产库素材、字号/字重变化、交互影响、浏览器验证结果和发布版本。若发现资产库没有对应图标，先生成或补充资产，再改页面引用；禁止在页面里临时画一个相似但未登记的图标。

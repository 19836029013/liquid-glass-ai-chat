org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.nonTransitiveRClass=true
android.useAndroidX=true
# The project lives under D:\档案与数据\DHS\dsh-remote (non-ASCII path). AGP refuses such
# paths by default; the build itself is unaffected, so the path check is disabled here.
android.overridePathCheck=true
